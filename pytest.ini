[pytest]
# Only look for tests in the tests/ directory
testpaths = tests

# Add src/ to sys.path so imports like `from src.core...` work during tests
pythonpath = src

# Ignore scripts and virtualenvs
norecursedirs = .venv venv scripts

markers =
	integration: mark test as an integration test that may require network or credentials
